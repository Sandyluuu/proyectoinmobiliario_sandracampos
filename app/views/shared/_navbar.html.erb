<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to image_tag('loguito.jpg', id: 'loguito', class: 'navbar-brand'), root_path %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Publica Aquí', new_property_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Propiedades', properties_path, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Iniciar Sesión', new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <div class="navbar-nav me-auto mb-2">
          <%= button_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
        </div>
      <% end %>
    </div>
  </div>
</nav>