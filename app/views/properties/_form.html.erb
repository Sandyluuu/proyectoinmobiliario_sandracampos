<%= form_with(model: property) do |form| %>
  <% if property.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(property.errors.count, "error") %> prohibited this property from being saved:</h2>

      <ul>
        <% property.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
    <div class="field">
      <%= form.label :property_type %>
      <%= form.select :property_type, options_for_select(['Departamento', 'Casa']) %>
    </div>
 

  <div>
    <%= form.label :operation_type, style: "display: block" %>
    <%= form.select :operation_type, options_for_select(['Arriendo', 'Compra', 'Venta']) %>
  </div>

  <div>
    <%= form.label :square_meters, style: "display: block" %>
    <%= form.text_field :square_meters %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.label :contact, style: "display: block" %>
    <%= form.text_field :contact %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
